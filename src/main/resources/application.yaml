spring:
  application:
    name: reactor-adapter-kit

server:
  port: 8080

logging:
  level:
    com.veldev.reactor_adapter_kit: INFO
    org.springframework.web: INFO
  pattern:
    console: "%d{HH:mm:ss} %-5level [%15.15t] %-40.40logger{39} : %msg%n"